#include "hwlib.hpp"
#include "../send_Franky/send.hpp"
#include "../Game/hit_control.cpp"
// couten naar de terminal
// in elke cout 2000 ms wacht tijd

// playerdata "tab" hits

struct playerhits   {
    int hit.data
    int player_nr
};

class USB_poort {
public:
    

};

for(int i = 0; i < valid; i++){
hwlib::wait_ms(2000);

hwlib::cout << "Player: " << playerhits.player_nr << "\n"<< "Number of hits " << playerhits.hit << hwlib::endl;
};
